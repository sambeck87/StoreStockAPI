en:
  errors:
    user_inactive: "User account is inactive"
    missing_parameter: "Required parameters are missing"
    validation_failed: "There are validation errors"
    not_found: "Resource not found"
    dependency_violation: "The resource cannot be deleted because it has associated dependencies"
    authentication:
      invalid_credentials: "Invalid credentials"
    authorization:
      forbidden: "You are not allowed to perform this action"
    unauthorized: "You are not authorized to perform this action"
    branch:
      main_branch_manager_immutable: "The manager of the main branch cannot be changed"
    user:
      has_managed_branches: "The user cannot be deleted because they manage one or more branches"

  activerecord:
    errors:
      messages:
        record_invalid: "Validation failed"
      models:
        user:
          attributes:
            email:
              blank: "can't be blank"
              taken: "is already taken"
            password:
              blank: "can't be blank"
              too_short: "is too short"
              invalid: "the password must contain at least 8 characters, one uppercase letter, one lowercase letter, and one number"
            password_confirmation:
              blank: "can't be blank"
              confirmation: "doesn't match confirmation"
            full_name:
              blank: "can't be blank"
        category:
          attributes:
            name:
              blank: "can't be blank"
              taken: "category already exists in this store"
        role:
          attributes:
            name:
              blank: "can't be blank"
              taken: "role already exists in this store"
        global_permission:
          attributes:
            name:
              blank: "can't be blank"
              taken: "global permission already exists in this store"